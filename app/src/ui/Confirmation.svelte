<script>
      export let title
      export let okLabel = "OK"
      export let cancelLabel = "Cancel"
      export let confirmation
      export let cancel
      export let show
</script>

<!-- 
<Dialog
  title={title}
  open={show}
>
  {confirmation}
  <Button
  label={cancelLabel}
  secondary={true}
  onClick={cancel}
/>,
<Button
  label={okLabel}
  primary={true}
  onClick={proceed}
/>

</Dialog> -->
