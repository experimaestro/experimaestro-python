[tox]
envlist = py36, py37

[testenv]
deps =
    pytest
	click
	fasteners
	jsonstreams
	websockets    
commands =
	mkdir -p test-reports/{envname}
    pytest --junitxml=test-reports/{envname}/junit.xml experimaestro/tests